import 'package:flutter/material.dart';

// Default padding value used throughout the application
const defaultPadding = 20.0;

// Horizontal and vertical padding constants derived from defaultPadding
const double horizontalPadding = defaultPadding;
const double verticalPadding = defaultPadding / 2;

// Background color for the app
const backgroundColor = Color(0xFFF1EFF1);

// Main color theme for the app
const appMainColor = Color(0xff429689);
